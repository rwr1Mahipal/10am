#include <stdio.h>
#include <string.h>

int main()
{
    //   char ch = 'A';
    //   printf("%c",ch);
    // char str[100] = "Mern Developer";
    // char str1[100] = " Java Developer";
    // char ch[100];
    //   printf("\n%s",str);
    //   char name[] = {"Mern Developer 2"};
    //   printf("\n%s",name);

    // char str[100];
    // printf("Enter string: ");
    // // scanf("%s",&str);
    // fgets(str, 100, stdin);
    // // printf("\n%s", str);
    // puts(str);

    for (char ch = 'A'; ch < 'Z'; ch++)
    {
        printf("%c => ", ch);
        printf("%d ",ch);
    }

    // printf("%d ",strlen(str));
    // printf("%s ",strlwr(str));
    // printf("%s ",strupr(str));
    // strcpy(ch, str);
    // printf("%s", ch);
    // strcat(str,str1);
    // puts(str);
//     strncpy(str,"java",3);
// puts(str);
}